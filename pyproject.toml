[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "execguard"
version = "0.4.0"
requires-python = ">=3.10"

[project.scripts]
execguard = "execguard:main"

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
